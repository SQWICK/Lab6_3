# Server configuration
server.port=5000
server.host=localhost

# Collection configuration
collection.file=collection.xml

# Client configuration
client.timeout=5000
client.buffer.size=4096

# Logging configuration
logging.level=INFO
logging.file=logs/application.log 